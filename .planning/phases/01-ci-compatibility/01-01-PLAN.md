---
phase: 01-ci-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/daily.yml
autonomous: false
requirements:
  - CI-01
user_setup: []

must_haves:
  truths:
    - "GitHub Actions workflow runs without Node.js 20 deprecation warnings"
    - "All action versions in daily.yml reference Node.js 24-compatible releases"
  artifacts:
    - path: ".github/workflows/daily.yml"
      provides: "Daily automation workflow with Node.js 24-compatible actions"
      contains: "actions/checkout@v6"
  key_links:
    - from: ".github/workflows/daily.yml"
      to: "GitHub Actions runner"
      via: "actions/checkout@v6, actions/cache@v5, actions/setup-python@v6"
      pattern: "actions/(checkout@v6|cache@v5|setup-python@v6)"
---

<objective>
Upgrade all GitHub Actions in the daily workflow to Node.js 24-compatible versions, eliminating deprecation warnings and ensuring the pipeline continues to work after the June 2, 2026 hard deadline.

Purpose: The current workflow uses actions/checkout@v4, actions/cache@v4, and actions/setup-python@v5, all of which run on Node.js 20 (deprecated, EOL April 2026, forced migration June 2, 2026).
Output: Updated .github/workflows/daily.yml with all four action references upgraded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ci-compatibility/01-RESEARCH.md
@.github/workflows/daily.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade GitHub Actions to Node.js 24-compatible versions</name>
  <files>.github/workflows/daily.yml</files>
  <read_first>
    - .github/workflows/daily.yml (current workflow — verify exact line content before editing)
    - .planning/phases/01-ci-compatibility/01-RESEARCH.md (confirmed target versions)
  </read_first>
  <action>
In .github/workflows/daily.yml, make these four substitutions (per CI-01):

1. Line 24: change `uses: actions/checkout@v4` to `uses: actions/checkout@v6`
2. Line 29: change `uses: actions/cache@v4` to `uses: actions/cache@v5` (apt packages cache)
3. Line 43: change `uses: actions/cache@v4` to `uses: actions/cache@v5` (Piper TTS cache)
4. Line 60: change `uses: actions/setup-python@v5` to `uses: actions/setup-python@v6`

IMPORTANT: There are TWO actions/cache references (apt packages on line 29, Piper TTS on line 43). Both must be updated. Do not change any other lines — step order, `with:` parameters, env vars, and run scripts remain identical.

Target versions (from research):
- actions/checkout@v6 (v6.0.2+ confirmed Node.js 24 compatible)
- actions/cache@v5 (v5.0.4+ confirmed Node.js 24 compatible)
- actions/setup-python@v6 (v6.2.0+ confirmed Node.js 24 compatible)
  </action>
  <acceptance_criteria>
    - `grep -c "actions/checkout@v6" .github/workflows/daily.yml` returns `1`
    - `grep -c "actions/cache@v5" .github/workflows/daily.yml` returns `2`
    - `grep -c "actions/setup-python@v6" .github/workflows/daily.yml` returns `1`
    - `grep -c "actions/checkout@v4" .github/workflows/daily.yml` returns `0`
    - `grep -c "actions/cache@v4" .github/workflows/daily.yml` returns `0`
    - `grep -c "actions/setup-python@v5" .github/workflows/daily.yml` returns `0`
    - Total line count of daily.yml is unchanged (91 lines)
  </acceptance_criteria>
  <verify>
    <automated>grep -cE "actions/(checkout@v4|cache@v4|setup-python@v5)" .github/workflows/daily.yml | grep -q "^0$" && grep -cE "actions/(checkout@v6|cache@v5|setup-python@v6)" .github/workflows/daily.yml | grep -q "^4$" && echo "PASS: All 4 action references upgraded" || echo "FAIL: Action version mismatch"</automated>
  </verify>
  <done>All four action references in daily.yml point to Node.js 24-compatible versions. Zero references to @v4 checkout, @v4 cache, or @v5 setup-python remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify workflow runs without deprecation warnings</name>
  <files>.github/workflows/daily.yml</files>
  <read_first>
    - .github/workflows/daily.yml (verify the upgraded versions are in place before triggering)
  </read_first>
  <action>
Push the updated daily.yml to main, then trigger a manual workflow run and inspect the logs for deprecation warnings. The executor should run:

1. `git push origin main` (ensure updated daily.yml is on remote)
2. `gh workflow run daily.yml` (trigger manual run)
3. `gh run watch` (wait for completion)
4. `gh run view --log | grep -i "node.js 20"` (search for deprecation warnings — expect zero matches)

If the workflow fails or deprecation warnings appear, report the specific error.
  </action>
  <what-built>Updated daily.yml with Node.js 24-compatible action versions (checkout@v6, cache@v5, setup-python@v6)</what-built>
  <how-to-verify>
    1. Push changes to main branch
    2. Trigger the workflow: `gh workflow run daily.yml`
    3. Watch the run: `gh run watch`
    4. Once complete, view logs: `gh run view --log`
    5. Confirm: NO lines containing "Node.js 20 actions are deprecated" appear in the output
    6. Confirm: Workflow completes successfully (green check)
  </how-to-verify>
  <acceptance_criteria>
    - Workflow run completes with a green check (exit code 0)
    - `gh run view --log | grep -ic "node.js 20 actions are deprecated"` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow=daily.yml --limit 1 --json conclusion -q ".[0].conclusion" | grep -q "success" && echo "PASS: Latest workflow run succeeded" || echo "FAIL: Latest workflow run did not succeed"</automated>
  </verify>
  <done>Live GitHub Actions workflow run completed successfully with zero Node.js 20 deprecation warnings.</done>
  <resume-signal>Type "approved" if workflow ran green with no deprecation warnings, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -cE "actions/(checkout@v4|cache@v4|setup-python@v5)" .github/workflows/daily.yml` returns 0 (no deprecated versions)
- `grep -cE "actions/(checkout@v6|cache@v5|setup-python@v6)" .github/workflows/daily.yml` returns 4 (all upgraded)
- Live workflow run completes without Node.js deprecation warnings (manual verification)
</verification>

<success_criteria>
1. daily.yml contains actions/checkout@v6, actions/cache@v5 (x2), actions/setup-python@v6
2. No references to actions/checkout@v4, actions/cache@v4, or actions/setup-python@v5 remain
3. Workflow runs successfully on GitHub Actions without Node.js 20 deprecation warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-ci-compatibility/01-01-SUMMARY.md`
</output>
